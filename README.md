#always build before pull request

#08-Jul-2024

## USE => `pnpm i` for install dependencies

## USE => `pnpm dev` for run local

## USE => `pnpm build` for production build & test any error before giving PULL REQUEST #20

develop branch: phase-2/develop
production branch: phase-2/master-production
